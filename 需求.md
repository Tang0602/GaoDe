你好，Claude。你的任务是扮演一位资深的安卓开发专家。我的应用在使用百度离线地图功能时，`MKOfflineMap.init()` 方法调用后一直没有回调，我怀疑是由于缺少 Android 6.0 以上版本的动态权限申请导致的。

**核心指令：**
请帮我在我的主 Activity (`MainActivity.kt`) 中添加完整的动态权限申请逻辑。离线地图功能需要 `WRITE_EXTERNAL_STORAGE` 和 `READ_EXTERNAL_STORAGE` 权限。

---

**具体实现要求：**

请为我的 `MainActivity.kt` 文件提供需要添加和修改的代码。代码逻辑应该实现以下流程：

1.  创建一个名为 `requestPermissions()` 的私有方法。
2.  在这个方法内部，检查 `WRITE_EXTERNAL_STORAGE` 和 `READ_EXTERNAL_STORAGE` 这两个权限是否已经被授予。
3.  如果存在任何一个未被授予的权限，则调用 `ActivityCompat.requestPermissions()` 来向用户发起请求。
4.  在 `MainActivity` 的 `onCreate` 方法中，在调用地图相关初始化代码**之前**，先调用这个新的 `requestPermissions()` 方法。
5.  **（可选但推荐）** 重写 `onRequestPermissionsResult` 方法，以便在用户做出授权选择后，可以打印日志或用 Toast 提示用户授权结果（成功或失败）。

---

**输出要求：**

请提供一个完整的代码块，展示如何在 `MainActivity.kt` 文件中集成上述的动态权限申请逻辑。请使用清晰的注释来标明哪些是需要新增或修改的代码，以便我能轻松地将它合并到我现有的文件中。

**例如，最终的 `onCreate` 方法结构应该类似于：**

```kotlin
override fun onCreate(savedInstanceState: Bundle?) {
    super.onCreate(savedInstanceState)
    setContentView(R.layout.activity_main)

    // 1. 先请求权限
    requestPermissions()

    // 2. 再设置离线地图
    setupOfflineMap()

    // ... 其他代码
}
```