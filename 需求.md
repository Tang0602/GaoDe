你好，Claude。你的任务是扮演一位资深的安卓开发专家，核心目标是帮助我将一个现有的安卓项目从**高德地图SDK**迁移到**百度地图SDK**。

**核心指令与最高约束：**
**你绝对禁止修改项目现有的任何UI布局、项目结构或应用框架。** 你的任务是一次**外科手术式的替换**，仅限于地图相关的组件和API调用。所有其他的业务逻辑代码、UI元素、XML布局都必须保持原样，不得触碰。你将严格遵循我提供的官方开发文档步骤来完成任务。

以下是具体的迁移步骤，请为每一步提供修改后完整的代码。

---

**第一步：配置 `AndroidManifest.xml` 文件**

请修改 `app/src/main/AndroidManifest.xml` 文件，完成以下操作：
1.  在 `<application>` 标签内部，添加百度地图的 `<meta-data>` 密钥配置。
2.  同时，找到并**移除**任何与高德地图相关的旧 `<meta-data>` 密钥配置。
3.  在 `<application>` 标签外部，添加百度地图所需的权限声明 (`INTERNET` 和 `ACCESS_NETWORK_STATE`)。
4.  检查并移除之前为高德地图添加的、现在不再需要的特定权限。

请在代码中使用这个密钥占位符：**`7jYFB0wgQ7HcYtVjAnpT3yKQiqSpTudR`**

---

**第二步：替换布局文件中的地图容器**

1.  定位到项目中包含地图视图的XML布局文件（例如 `activity_main.xml`）。这个文件里现在应该有一个高德的 `<com.amap.api.maps.MapView>`。
2.  请将这个 View **替换为**百度的 `<com.baidu.mapapi.map.MapView>`。
3.  并将其ID设置为 `android:id="@+id/bmapView"`。
4.  **约束：** 必须保持所有其他的UI元素、父布局、约束（ConstraintLayout）、边距（margin/padding）等所有属性完全不变。

---

**第三步：在Application类中初始化SDK**

1.  找到我项目中的自定义 `Application` 类（可能叫 `MyApplication`, `App` 等）。如果不存在，请创建一个名为 `DemoApplication` 的新类。
2.  在其 `onCreate` 方法中，添加百度SDK的初始化代码：`SDKInitializer.initialize(this);` 和 `SDKInitializer.setCoordType(CoordType.BD09LL);`。
3.  **非常重要：找到并删除或注释掉**旧的高德SDK初始化相关的代码。
4.  最后，确保这个自定义的 `Application` 类已在 `AndroidManifest.xml` 的 `<application>` 标签中通过 `android:name=".DemoApplication"` 进行了声明。

---

**第四步：在Activity/Fragment中管理MapView的生命周期**

1.  定位到展示地图的 Activity 或 Fragment（例如 `MainActivity`）。
2.  将该类中 `MapView` 成员变量的类型从高德的 `com.amap.api.maps.MapView` **更改为**百度的 `com.baidu.mapapi.map.MapView`。
3.  在 `onCreate` (或 `onViewCreated`) 方法中，更新 `findViewById` 的调用，使用新的ID `R.id.bmapView`，并将其强制转换为百度的 `MapView` 类型。
4.  在 `onResume`, `onPause`, `onDestroy` 这几个生命周期方法中，将原先调用高德 MapView 生命周期方法的地方，**替换为**调用百度 MapView 对应的方法。方法名完全相同，只需确保调用的是新的百度 MapView 对象的方法即可。

---

**最后一步：编写 `README.md` 文件**

请为本次迁移工作编写一份清晰的 `README.md` 文件。内容应包括：
1.  **迁移目标**：简述本次任务是将地图服务从高德SDK切换至百度SDK。
2.  **主要修改的文件列表**：列出所有被修改过的文件名（例如 `AndroidManifest.xml`, `activity_main.xml` 等）。
3.  **关键代码变更摘要**：简要说明每个文件中核心的修改内容（如：替换API Key、替换MapView组件、更新SDK初始化逻辑、调整生命周期管理代码）。
4.  **后续步骤提醒**：提醒开发者需要在 `AndroidManifest.xml` 中填入真实的百度地图AK，并检查其他与地图交互的业务逻辑代码（如添加标记点、路线规划等）是否需要进一步适配百度SDK的API。

**输出要求：**
请将所有修改后的文件代码，以及 `README.md` 文件的内容，分别放置在**清晰标记的代码块**中，方便我直接复制使用。