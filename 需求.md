**角色：** 你是一名资深的安卓开发与自动化测试架构师，负责创建一套完整的、文档化的测试解决方案。

**任务概述：**
为安卓App（包名：`com.example.GaoDe`）创建一个手动测试验证方案。你需要提供三项产出：**1. 安卓端的Kotlin代码**，**2. PC端的Python验证脚本**，以及 **3. 一个详细的`README.md`文件**来解释整个流程。

---

**核心架构与工作流程（必须严格遵守）：**

1.  **文件状态与位置（核心要求）：**
    *   一个名为 `1_message_history.json` 的JSON文件必须存在于App的**私有内部存储**中（路径：`/data/data/com.example.GaoDe/files/`）。
    *   **关键点：** 这个文件**不是**在用户首次操作时才被创建。App必须有**初始化逻辑**，确保在App第一次启动时，这个文件（可以带有一个空的JSON数组 `[]` 作为初始内容）就被创建在上述私有目录中。这保证了测试脚本在任何时候都能访问到一个已存在的文件。

2.  **测试流程：**
    *   **步骤A (人工操作):** 我，作为测试人员，**手动**在手机上操作App（例如，点击进入消息页面）。
    *   **步骤B (App记录):** 我的手动操作会触发App的原生Kotlin代码，向私有存储中的`1_message_history.json`文件追加一条新的、格式化的日志记录。
    *   **步骤C (脚本验证):** 我在PC上运行一个Python脚本，该脚本通过`adb`读取私有存储中的JSON文件，并验证最后一条记录是否正确。

**关键约束：**
*   **存储位置：** 严格限定在私有内部存储 (`/data/data/...`)。
*   **职责分离：** App负责初始化文件和写入日志；Python脚本**仅负责读取和验证**。
*   **无UI自动化：** Python脚本不包含任何UI交互或检查。

---

**你的具体任务：**

**任务一：提供安卓端的Kotlin代码**

编写一个Kotlin函数 `recordNavigationAction`，它：
1.  **实现文件初始化与写入：**
    *   在函数内部，首先检查`1_message_history.json`文件是否存在于`context.filesDir`。
    *   如果**不存在**，则立即创建一个包含初始内容（例如 `[]`）的新文件。
    *   然后，读取文件内容，追加一条新的JSON记录。
    *   最后，将包含新记录的**完整、格式化（带2个空格缩进）**的内容写回文件。
2.  **参数：** 接收 `Context`, `action`, `page` 作为输入。

**任务二：提供Python端的验证脚本**

编写一个Python函数 `verify_last_navigation_log`，它：
1.  使用精确的`adb`命令 `adb exec-out run-as com.example.GaoDe cat files/1_message_history.json` **直接将文件内容读入内存**。
2.  在内存中解析JSON，并验证最后一条记录的`action`字段是否与预期相符。
3.  在 `if __name__ == "__main__":` 中调用此函数，验证操作为 `"点击消息页面"`，并打印 `PASS` 或 `FAIL`。

**任务三：创建一个`README.md`文件**

创建一个名为 `README.md` 的Markdown文件，内容必须包含以下部分：

*   **## 测试流程概述**
    *   简要说明这是一个“手动操作，脚本验证”的流程。
*   **## 架构设计**
    *   **App端：** 解释App负责在其私有内部存储中维护一个`1_message_history.json`日志文件。强调App有初始化逻辑，确保此文件在首次启动后即存在。
    *   **测试端：** 解释Python脚本通过`adb run-as`命令来读取这个私有文件并进行验证。
*   **## 如何执行测试**
    *   提供一个清晰的步骤列表：
        1.  确保安卓设备已连接并开启USB调试。
        2.  在手机上，手动执行指定操作（例如：打开App并进入消息页面）。
        3.  在PC的`Autotest`文件夹中，打开终端。
        4.  运行命令：`python your_script_name.py`。
        5.  观察终端输出的 `PASS` 或 `FAIL` 结果。
*   **## 文件说明**
    *   **`your_script_name.py`:** 描述脚本的用途（读取设备私有日志并验证）。
    *   **`1_message_history.json` (设备端):** 描述它在设备上的精确路径 (`/data/data/com.example.GaoDe/files/`) 和作用。

请基于以上所有要求，开始生成这三项产出。