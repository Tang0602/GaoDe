问题描述:
应用在使用百度地图时发生崩溃，日志显示 FATAL EXCEPTION: GLThread，核心错误为 java.lang.RuntimeException: mEglConfig not initialized。
根本原因:
这是一个典型的百度地图SDK集成错误，意味着地图的渲染环境（EGL）在需要使用时未能成功初始化。通常由以下两点导致。
第一步：检查 MapView 的生命周期管理（最优先）
这是导致该问题的最常见原因（>95% 的可能性）。请严格核对包含 MapView 的 Activity 或 Fragment，确保其生命周期与 MapView 完全同步。
检查清单:
是否重写了 onResume() 方法？
是否在 onResume() 内部调用了 mMapView.onResume()？
是否重写了 onPause() 方法，并在其中调用了 mMapView.onPause()？
是否重写了 onDestroy() 方法，并在其中调用了 mMapView.onDestroy()？
正确代码示例 (Activity):
code
Java
public class MapActivity extends Activity {  
    private MapView mMapView = null;  

    @Override  
    protected void onCreate(Bundle savedInstanceState) {  
        super.onCreate(savedInstanceState);    
        setContentView(R.layout.activity_map);  
        mMapView = (MapView) findViewById(R.id.bmapView);  
    }  

    @Override  
    protected void onResume() {  
       super.onResume();  
       // 必须调用此方法，以恢复地图的绘制
       if (mMapView != null) {
           mMapView.onResume();
       }
    }  

    @Override  
    protected void onPause() {  
      super.onPause();  
      // 必须调用此方法，以暂停地图的绘制
      if (mMapView != null) {
          mMapView.onPause();
      }
    } 
    
    @Override  
    protected void onDestroy() {  
      super.onDestroy();  
      // 必须调用此方法，以销毁地图并释放资源
      if (mMapView != null) {
          mMapView.onDestroy();
      }
    }  
}
第二步：确认 SDK 已在 Application 中全局初始化
SDK 的核心服务需要在任何地图组件使用前被初始化。
检查清单:
项目中是否存在一个自定义的 Application 类？
是否在该类的 onCreate() 方法中调用了 SDKInitializer.initialize(this);？
该自定义 Application 类是否已在 AndroidManifest.xml 的 <application> 标签中通过 android:name 属性声明？
正确代码示例:
1. 自定义 Application 类 (MyApplication.java)
code
Java
public class MyApplication extends Application {
    @Override
    public void onCreate() {
        super.onCreate();
        // 在使用SDK各组件之前初始化context信息，传入ApplicationContext
        SDKInitializer.initialize(this);
    }
}
2. AndroidManifest.xml
code
Xml
<application
    android:name=".MyApplication" 
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    ...>
    
    <!-- 确保 API Key 已配置 -->
    <meta-data  
        android:name="com.baidu.lbsapi.API_KEY"  
        android:value="你的有效API_KEY" />
        
    <activity android:name=".MapActivity">
        ...
    </activity>

</application>
总结:
请严格按照以上两个步骤进行排查。首先解决 MapView 生命周期管理 问题，如果问题依旧，再检查 SDK 初始化。修复这两个集成点几乎可以 100% 解决 mEglConfig not initialized 崩溃。