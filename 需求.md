给AI的Prompt (用于重构 onPOIClick 回调)
角色： 你是一名精通Jetpack Compose和Kotlin架构的安卓开发专家。我需要你为我重构一个事件回调，使其能够传递更丰富的数据对象，以解决一个逻辑判断问题。
任务概述：
在我的POIResultsListScreen.-kt中，当用户点击列表项时，onPOIClick回调目前只返回一个无法识别的poi.id字符串。这导致我在MainActivity.kt中无法根据地点名称来执行条件日志记录。你需要重构POIResultsListScreen.kt和MainActivity.kt，让onPOIClick回调能够传递整个POI数据对象。
第一部分：这是我现有的、需要被重构的 POIResultsListScreen.kt 的关键代码：
code
Kotlin
// --- 我现有的 POIResultsListScreen.kt ---

// (假设在文件顶部有一个 POI 数据类的定义)
// data class POI(val id: String, val name: String, ...)

@Composable
fun POIResultsListScreen(
    // ... 其他参数 ...
    onPOIClick: (String) -> Unit // <-- 问题1: 签名只接受 String
) {
    // ...
    LazyColumn(...) {
        items(items = poiList) { poi ->
            POILIstItem(
                poi = poi,
                onClick = { onPOIClick(poi.id) } // <-- 问题2: 调用时只传递了 poi.id
            )
            Divider(...)
        }
    }
    // ...
}
第二部分：这是我现有的、需要被重构的 MainActivity.kt 的 NavHost 部分代码：
code
Kotlin
// --- 我现有的 MainActivity.kt ---

// ...
composable("${Screen.POIResultsList.route}/{category}") { backStackEntry ->
    val category = ...
    val context = ...
    POIResultsListScreen(
        // ...
        onPOIClick = { poiId -> // <-- 问题3: 这里接收的是 poiId，无法获取名字
            // 这个 if 判断永远不会成功
            if (poiId.contains("banu", ignoreCase = true)) {
                MainActivity.recordLog(...)
            }
            navController.navigate("${Screen.ShowPlaceDetails.route}/${poiId}")
        }
    )
}
// ...
你的具体任务：
你需要提供两个修改后的完整文件来完成这次重构。
任务一：重构 POIResultsListScreen.kt
请提供一个完整的、重构后的 POIResultsListScreen.kt 文件。这个文件必须包含以下所有修改：
修改函数签名：
将POIResultsListScreen函数的onPOIClick参数类型从(String) -> Unit修改为(POI) -> Unit。
修改回调的调用点：
在LazyColumn的items循环中，找到POILIstItem的onClick参数。
将其实现从{ onPOIClick(poi.id) }修改为{ onPOIClick(poi) }，以传递整个poi对象。
任务二：重构 MainActivity.kt
请提供一个完整的、重构后的 MainActivity.kt 文件。主要的修改集中在MainScreen函数的NavHost内部。
修改POIResultsListScreen的调用：
在NavHost中，找到composable("${Screen.POIResultsList.route}/{category}")这个代码块。
修改传递给POIResultsListScreen的onPOIClick回调的实现。
新的Lambda参数名应该从poiId改为poi，以反映它现在是一个POI对象。
修改if条件判断，使其**基于poi.name**而不是poi.id。新的条件应该是：if (poi.name.contains("巴奴", ignoreCase = true))。
确保导航逻辑仍然使用poi.id：navController.navigate("${Screen.ShowPlaceDetails.route}/${poi.id}")。
最终产出：
一个完整的、可以直接替换的 POIResultsListScreen.kt 文件。
一个完整的、可以直接替换的 MainActivity.kt 文件。
这两个文件结合起来，应该能彻底解决指令#8的日志记录问题。