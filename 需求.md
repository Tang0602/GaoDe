你好，Claude。我们需要对App进行一次重大功能升级。之前的路线规划界面显示的信息太过简化，现在我们需要**严格按照范例**（'UIReference/样式.png'），实现一个信息极其丰富的公共交通路线展示界面。同时，我们需要根据定位结果动态更新起点的显示名称。

请严格遵守以下所有要求和约束。

**核心原则（必须严格遵守）：**
你依然是一个**数据和SDK逻辑实现者**。UI模板已经存在，你的任务是用从SDK获取的、经过复杂解析后的真实数据，去填充这些UI组件。**绝对禁止修改任何XML布局文件或UI框架。**

---

### **任务分解**

#### **第1步：实现起点的“条件显示”与真实定位**

**目标**：在POI详情页，根据定位成功与否，显示不同的起点名称。

**实现细节**：
1.  **集成高德定位SDK**：在POI详情页Activity中，集成并初始化高德定位SDK (`AMapLocationClient`)。
2.  **设置定位回调监听**：实现`onLocationChanged(AMapLocation amapLocation)`回调方法。
3.  **实现条件显示逻辑**：
    *   在`onLocationChanged`回调中，获取显示起点的`TextView`。
    *   **如果定位成功** (`amapLocation.getErrorCode() == 0`)：
        *   将`TextView`的文本设置为 **“华中师范大学（南湖校区）”**。
        *   **注意**：这**仅仅是UI显示**！用于后续路线规划的起点坐标 **仍然必须使用** 我们固定的坐标 `new LatLonPoint(30.518000, 114.363000)`，以确保本次任务的可测试性。
    *   **如果定位失败**：
        *   将`TextView`的文本设置为 **“我的位置”**。
        *   在用户点击“路线”时，可以给出一个Toast提示“定位失败，无法规划路线”，或者仍然使用固定坐标进行规划。
4.  **启动定位**：在进入详情页时，调用`mLocationClient.startLocation();`来获取一次位置。

---

#### **第2步：实现超详细的公共交通路线规划与数据解析**

**目标**：在路线规划页，将从高德SDK返回的`BusRouteResult`数据，解析成如图2所示的丰富样式并填充UI。

**实现细节**：
1.  **接收数据**：在路线规划页Activity中，从`Intent`中获取起点和终点的`LatLonPoint`对象。
2.  **调用SDK查询**：这部分逻辑不变，使用`calculateBusRouteAsyn()`发起公交路线查询。
3.  **超详细解析与填充（最关键的一步）**：
    *   在`onBusRouteSearched(BusRouteResult result, int rCode)`回调中，你将进行极其精细的数据解析。
    *   **A. 遍历路线方案 (`List<BusPath>`)**: `result.getPaths()`返回的每一个`BusPath`都对应UI上的一张卡片。
    *   **B. 填充卡片顶层信息**: 对于每一个`BusPath`，首先填充卡片的宏观信息：
        *   总耗时: `busPath.getDuration()`
        *   总步行距离 (如图2的“步行 1.9公里”): `busPath.getWalkDistance()`，你需要将这个数据显示在卡片顶部。
    *   **C. 深入解析每一段路程 (`List<BusStep>`)**: 这是实现图2效果的核心。你需要遍历`busPath.getSteps()`，对每个`BusStep`进行判断和数据提取：
        *   **如果是公交或地铁段** (`busStep.getBusLines()`不为空)：
            *   遍历`busStep.getBusLines()`中的每一个`BusLineItem`。
            *   **线路名称**: 获取`busLineItem.getBusLineName()` (例如“汉奇定制公交4号线”)。
            *   **经过站数**: 获取`busLineItem.getPassStationNum()` (例如“15站”)。
            *   **上车站**: 获取`busLineItem.getDepartureBusStation().getBusStationName()`。
            *   你需要将这些信息组合起来，动态创建如图2所示的、带有公交/地铁图标和线路名称的视图。
        *   **如果是步行段** (`busStep.getWalk() != null`)：
            *   你可以提取步行距离`busStep.getWalk().getDistance()`，并在公交/地铁段之间显示一个步行图标和距离。
    *   **D. 实现高级逻辑 (特殊信息提示)**:
        *   **首/末班车提示**: 对于每个`BusLineItem`，可以通过`getFirstBusTime()`和`getLastBusTime()`获取首末班车时间。你需要将这个时间与当前系统时间进行比较，如果接近首班车或末班车时间，就构造出如图2所示的“⚠️ 首班车即将发出”或“末班车时间XX:XX”的提示语。
        *   **“当前不可乘坐”方案**: 在所有路线方案遍历完成后，你需要实现一个逻辑：检查每个`BusPath`的首段公交/地铁的`getFirstBusTime()`。如果该时间晚于当前时间太多，或者`getLastBusTime()`已经早于当前时间，就将这个`BusPath`从主列表移除，并添加到UI底部一个独立的“以下方案当前不可乘坐”的区域中。

---

### **最终要求**

1.  **UI填充**：你的最终目标是让路线规划页面的UI，在数据丰富程度上，无限接近截图2的效果。
2.  **更新文档**：在`README.md`中更新“核心功能实现”章节，详细描述你新增的“条件显示起点名称”逻辑，以及“精细化解析公交路线数据”的实现方法。
3.  **遵守约束**：再次强调，**绝对不要修改UI的XML布局和App的既定框架**。

请开始执行这个更具挑战性的任务。