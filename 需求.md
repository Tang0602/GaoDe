### **给 Claude Code 的最终版、超详细 Prompt**

你好，Claude。你的任务是为一个已存在的、UI和框架都已固定的Android高德地图应用，注入真实的数据和功能。

**核心原则（必须严格遵守）：**
你的角色**不是**一个UI设计师或架构师，而是一个**纯粹的数据和SDK逻辑实现者**。你必须假设App的UI界面和代码结构都是完美的，你的唯一工作就是在指定的位置，通过调用高德地图SDK的在线接口，获取数据并填充到这些已存在的视图中。

**绝对禁止以下行为：**
*   **禁止修改任何XML布局文件。**
*   **禁止修改任何UI组件的样式、颜色、大小或位置。**
*   **禁止修改任何Activity或Fragment的继承关系、类名或项目的包结构。**

你的所有代码都将是“填空题”，而不是“作文题”。

---

### **完整实现流程**

我们将分三步，完整实现一个真实的用户场景：**“定位用户当前位置 -> 查看POI详情 -> 规划公共交通路线”**。

#### **第1步：实现“我的位置”的真实定位**

**目标**：在发起路线规划时，起点不再是固定坐标，而是通过高德定位SDK获取到的用户设备的实时位置。

**实现细节**：
1.  你需要在将要发起路线规划的Activity中（通常是POI详情页），集成高德定位SDK。
2.  **初始化定位客户端**：
    ```java
    // 声明AMapLocationClient类对象
    public AMapLocationClient mLocationClient = null;
    // 初始化定位
    mLocationClient = new AMapLocationClient(getApplicationContext());
    ```
3.  **设置定位回调监听**：创建一个`AMapLocationListener`，并实现其`onLocationChanged(AMapLocation amapLocation)`方法。
4.  **处理定位结果**：在`onLocationChanged`方法中，检查`amapLocation.getErrorCode() == 0`以确认定位成功。成功后，从中提取经纬度，并存储为一个`LatLonPoint`类型的成员变量，例如 `private LatLonPoint myLocationPoint;`。
    ```java
    if (amapLocation != null && amapLocation.getErrorCode() == 0) {
        // 定位成功，获取经纬度
        myLocationPoint = new LatLonPoint(amapLocation.getLatitude(), amapLocation.getLongitude());
    }
    ```
5.  **启动定位**：在需要获取位置时（例如，在用户点击“路线”按钮之前），调用`mLocationClient.startLocation();`来启动单次定位。

---

#### **第2步：实现“巴奴毛肚火锅”详情页的真实数据填充**

**目标**：当用户进入详情页时，使用高德搜索SDK在线查询该POI的详细信息，并用返回的真实数据动态更新UI。

**实现细节**：
1.  **数据源**：我们约定，详情页Activity会接收到一个由上一个页面传递过来的POI ID。本次任务中，请直接在详情页代码中使用这个固定的ID：`B0FFG1QA5P`。
2.  **调用SDK进行ID搜索**：你必须严格按照以下步骤，使用**高德搜索SDK**发起一次在线的ID搜索请求。
    *   **A. 初始化PoiSearch**: `PoiSearch poiSearch = new PoiSearch(this, null);`
    *   **B. 设置监听器**: `poiSearch.setOnPoiSearchListener(this);`
    *   **C. 发起异步请求**: `poiSearch.searchPOIIdAsyn("B0FFG1QA5P");`
3.  **解析并填充数据**：在`onPoiItemSearched(PoiItem poiItem, int rCode)`这个回调方法中，检查`rCode`为1000（成功）后，从返回的`poiItem`对象中解析数据，并更新对应的UI组件。对应关系如下：
    *   店铺名称文本框 -> `poiItem.getTitle()`
    *   分类文本框 -> `poiItem.getTypeDes()`
    *   评分视图 -> `poiItem.getPoiExtension().getRating()`
    *   营业时间文本框 -> `poiItem.getPoiExtension().getOpentime()`
    *   地址文本框 -> `poiItem.getSnippet()`
    *   电话按钮/文本框 -> `poiItem.getTel()`
4.  **重要提示**：对于UI模板中，SDK无法提供的数据（如“连续2年>”、“街边口火锅粉”、菜品列表、用户评价），**必须保持其虚构的占位数据不变**。

---

#### **第3步：实现到“巴奴毛肚火锅”的公共交通路线规划**

**目标**：当用户在详情页点击“路线”按钮后，跳转到路线规划页，并在线查询从“我的位置”（第1步获取）到该火锅店的公共交通路线，并用真实数据填充UI。

**实现细节**：
1.  **数据传递**：在详情页点击“路线”按钮的事件中，将第1步获取到的`myLocationPoint`（起点）和第2步`onPoiItemSearched`回调中获取到的`poiItem.getLatLonPoint()`（终点），通过`Intent`传递给路线规划页Activity。
2.  **调用SDK进行公交路线规划**：在路线规划页Activity中，你必须严格按照以下步骤，使用**高德路线规划SDK**发起在线请求。
    *   **A. 初始化RouteSearch**: `RouteSearch routeSearch = new RouteSearch(this);`
    *   **B. 设置监听器**: `routeSearch.setRouteSearchListener(this);`
    *   **C. 构造查询参数**:
        *   从`Intent`中获取起点和终点的`LatLonPoint`。
        *   使用这两个点创建`RouteSearch.FromAndTo`对象。
        *   创建一个`RouteSearch.BusRouteQuery`对象，策略使用默认的`RouteSearch.BUS_DEFAULT`。
    *   **D. 发起异步请求**: `routeSearch.calculateBusRouteAsyn(query);`
3.  **解析并填充数据**：在`onBusRouteSearched(BusRouteResult result, int rCode)`这个回调方法中，检查`rCode`为1000（成功）后，开始解析极其复杂的`BusRouteResult`对象。
    *   通过`result.getPaths()`可以获得一个`List<BusPath>`，列表中的每一个`BusPath`对象都对应UI上的一张路线方案卡片。
    *   你需要遍历这个列表，对于每一个`BusPath`，提取以下信息来填充一张卡片：
        *   **总耗时**: `busPath.getDuration()` (秒)，需格式化。
        *   **总距离**: `busPath.getDistance()` (米)，需格式化。
        *   **总费用**: `busPath.getCost()` (元)。
        *   **路线详情（最复杂的部分）**: 你需要遍历`busPath.getSteps()`，它是一个`List<BusStep>`。对于每一个`BusStep`，判断它是步行、公交还是地铁，然后提取出具体的线路名称（如`busLine.getBusLineName()`）、经过的站数（`busLine.getPassStationNum()`）、上/下车站名等信息，拼接成UI上显示的详细路径描述。

---

### **最终要求与文档更新**

1.  **代码注释**：在你编写的所有新代码块上方，必须添加清晰的中文注释，解释这段代码的目的，尤其是在解析SDK返回结果的部分。
2.  **更新文档**：请在项目根目录的 `README.md` 文件中，新增一个名为 "## 核心功能实现：实时定位与POI路线规划" 的章节，简要描述本次添加的“定位->详情->公交规划”的完整业务流程和SDK调用逻辑。

请严格按照以上所有步骤和细节执行。