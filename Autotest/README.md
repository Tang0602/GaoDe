# Android App Manual Testing with Script Verification

## 测试流程概述

这是一个"手动操作，脚本验证"的测试流程。测试人员在手机上手动操作App，App记录操作日志，然后通过Python脚本验证操作是否被正确记录。

## 架构设计

### App端
- **职责**: App负责在其私有内部存储中维护一个`1_message_history.json`日志文件
- **初始化逻辑**: App具有初始化逻辑，确保此文件在首次启动后即存在于 `/data/data/com.example.GaoDe/files/` 目录中
- **记录机制**: 当用户执行特定操作（如点击进入消息页面）时，App自动向JSON文件追加格式化的操作记录
- **存储位置**: 严格限定在私有内部存储 (`/data/data/com.example.GaoDe/files/1_message_history.json`)

### 测试端
- **职责**: Python脚本仅负责读取和验证操作日志，不包含任何UI交互
- **读取机制**: 通过 `adb run-as` 命令访问App的私有存储文件
- **验证逻辑**: 在内存中解析JSON数据，验证最后一条记录的操作是否符合预期

## 如何执行测试

### 步骤 1: 环境准备
确保安卓设备已连接并开启USB调试模式

### 步骤 2: 手动操作
在手机上手动执行指定操作：
- 打开App（包名：`com.example.GaoDe`）
- 点击底部导航栏的"消息"选项卡
- 进入消息页面

### 步骤 3: 进入测试目录
在PC上进入`Autotest`文件夹：
```bash
cd Autotest
```

### 步骤 4: 执行验证脚本
运行验证脚本：
```bash
python verify_navigation_log.py
```

### 步骤 5: 查看结果
观察终端输出的测试结果：
- `✓ PASS` - 验证成功
- `✗ FAIL` - 验证失败

### 用例二：验证"查看我的收藏"
1. 在手机上，手动点击"我的"进入个人中心，然后点击"收藏夹"。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_2.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例三：验证"搜索美食"
1. 在手机上，手动点击"首页"，点击搜索框，输入"美食"并执行搜索。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_3.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例四：验证"进入主页"
1. 在手机上，手动执行：打开App。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_4.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例五：验证"查看个人中心"
1. 在手机上，手动执行：点击"我的"。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_5.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例六：验证"进入与妈妈的聊天界面"
1. 在手机上，手动执行：点击"消息"-"妈妈"。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_6.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例七：验证"点击回家按钮"
1. 在手机上，手动执行：点击"首页"-"回家"。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_7.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例八：验证"搜索并选择巴奴火锅"
1. 在手机上，手动执行：点击"首页"-"底下的搜索框"-"美食"-"巴奴毛肚火锅（群光广场店）"。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_8.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例九：验证"打车去东湖风景区"
1. 在手机上，手动执行：点击"首页"-"搜索框"-"景点门票"-"东湖风景区"-"路线"-选择"打车"-选择价位并勾选类型-"立即打车"。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_9.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例十：验证"预订汉庭酒店"
1. 在手机上，手动执行：点击"首页"-"搜索框"-点击"汉庭酒店"下的"订购"。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_10.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例十一：验证"收藏老乡鸡餐厅"
1. 在手机上，手动执行：点击"首页"-"搜索框"-"美食"-"老乡鸡（珞喻路店）"-"收藏"。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_11.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例十二：验证"发送与爸爸的聊天，询问吃了吗"
1. 在手机上，手动执行：点击"消息"-"爸爸"-发送'吃了吗'。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_12.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例十三：验证"经济型打车去武汉欢乐谷"
1. 在手机上，手动执行：点击"首页"-"搜索框"-"景点门票"-"欢乐谷"-"路线"-选择"打车"-选择经济-勾选任意车辆-立即打车。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_13.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例十四：验证"分享如家酒店位置给妈妈"
1. 在手机上，手动执行：点击"首页"-"搜索框"-点击"如家酒店"-分享-选择妈妈。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_14.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

### 用例十五：验证"公交导航去木屋烧烤"
1. 在手机上，手动执行：点击"首页"-"搜索框"-"美食"-"老乡鸡（珞喻路店）"-"路线"-选择公共交通-点击18分钟的路线。
2. 在PC的`Autotest`文件夹中，运行命令：`python eval_15.py`。
3. 观察终端输出的 `PASS` 或 `FAIL` 结果。

## 文件说明

### `verify_navigation_log.py`
- **用途**: 读取设备私有日志并验证最后一条操作记录
- **功能**: 
  - 通过ADB命令直接读取私有存储中的JSON文件
  - 解析JSON数据并验证最后一条记录的`action`字段
  - 输出详细的验证结果和调试信息
- **预期验证**: 验证操作为"点击消息页面"

### `eval_2.py`
- **用途**: 用于验证"查看我的收藏"操作的日志是否正确记录。
- **功能**:
  - 通过ADB命令读取私有存储中的`2_favorites_history.json`文件
  - 解析JSON数据并验证最后一条记录的`action`字段是否为"查看我的收藏"
  - 输出详细的验证结果和调试信息
- **预期验证**: 验证操作为"查看我的收藏"

### `eval_3.py`
- **用途**: 用于验证"搜索美食"操作的日志是否正确记录。
- **功能**:
  - 通过ADB命令读取私有存储中的`3_food_search_history.json`文件
  - 解析JSON数据并验证最后一条记录的`action`字段是否为"搜索美食"
  - 输出详细的验证结果和调试信息
- **预期验证**: 验证操作为"搜索美食"

### `eval_4.py`
- **用途**: 用于验证"进入主页"操作的日志是否正确记录。

### `eval_5.py`
- **用途**: 用于验证"查看个人中心"操作的日志是否正确记录。

### `eval_6.py`
- **用途**: 用于验证"进入与妈妈的聊天界面"操作的日志是否正确记录。

### `eval_7.py`
- **用途**: 用于验证"点击回家按钮"操作的日志是否正确记录。

### `eval_8.py`
- **用途**: 用于验证"搜索并选择巴奴火锅"操作的日志是否正确记录。

### `eval_9.py`
- **用途**: 用于验证"打车去东湖风景区"操作的日志是否正确记录。

### `eval_10.py`
- **用途**: 用于验证"预订汉庭酒店"操作的日志是否正确记录。

### `eval_11.py`
- **用途**: 用于验证"收藏老乡鸡餐厅"操作的日志是否正确记录。

### `eval_12.py`
- **用途**: 用于验证"发送与爸爸的聊天，询问吃了吗"操作的日志是否正确记录。

### `eval_13.py`
- **用途**: 用于验证"经济型打车去武汉欢乐谷"操作的日志是否正确记录。

### `eval_14.py`
- **用途**: 用于验证"分享如家酒店位置给妈妈"操作的日志是否正确记录。

### `eval_15.py`
- **用途**: 用于验证"公交导航去木屋烧烤"操作的日志是否正确记录。

### `1_message_history.json` (设备端)
- **位置**: `/data/data/com.example.GaoDe/files/1_message_history.json`
- **作用**: 存储App用户操作的历史记录
- **格式**: JSON数组，包含带有时间戳、操作和页面信息的记录
- **示例格式**:
```json
[
  {
    "timestamp": "2025-11-09 14:30:25",
    "action": "点击消息页面",
    "page": "MessageScreen"
  }
]
```

### `2_favorites_history.json` (设备端)
- **位置**: `/data/data/com.example.GaoDe/files/2_favorites_history.json`
- **作用**: 记录用户查看收藏夹历史的日志文件。
- **格式**: JSON数组，包含带有时间戳、操作和页面信息的记录
- **示例格式**:
```json
[
  {
    "timestamp": "2025-11-09 14:30:25",
    "action": "查看我的收藏",
    "page": "收藏夹页面"
  }
]
```

### `3_food_search_history.json` (设备端)
- **位置**: `/data/data/com.example.GaoDe/files/3_food_search_history.json`
- **作用**: 记录用户搜索美食历史的日志文件。
- **格式**: JSON数组，包含带有时间戳、操作和页面信息的记录
- **示例格式**:
```json
[
  {
    "timestamp": "2025-11-09 14:30:25",
    "action": "搜索美食",
    "page": "美食搜索结果页"
  }
]
```

### `4_home_navigation_history.json` (设备端)
- **用途**: 记录进入主页历史的日志文件。

### `5_profile_history.json` (设备端)
- **用途**: 记录查看个人中心历史的日志文件。

### `6_chat_history.json` (设备端)
- **用途**: 记录进入与妈妈的聊天界面历史的日志文件。

### `7_home_navigation_history.json` (设备端)
- **用途**: 记录点击回家按钮历史的日志文件。

### `8_banu_selection_history.json` (设备端)
- **用途**: 记录搜索并选择巴奴火锅历史的日志文件。

### `9_donghu_ride_history.json` (设备端)
- **用途**: 记录打车去东湖风景区历史的日志文件。

### `10_hanting_booking_history.json` (设备端)
- **用途**: 记录预订汉庭酒店历史的日志文件。

### `11_favorite_restaurant_history.json` (设备端)
- **用途**: 记录收藏老乡鸡餐厅历史的日志文件。

### `12_dad_chat_history.json` (设备端)
- **用途**: 记录发送与爸爸的聊天，询问吃了吗历史的日志文件。

### `13_happy_valley_ride_history.json` (设备端)
- **用途**: 记录经济型打车去武汉欢乐谷历史的日志文件。

### `14_hotel_share_history.json` (设备端)
- **用途**: 记录分享如家酒店位置给妈妈历史的日志文件。

### `15_muyu_navigation_history.json` (设备端)
- **用途**: 记录公交导航去木屋烧烤历史的日志文件。

## 核心特性

1. **无UI自动化依赖**: 测试脚本不包含任何UI交互或检查
2. **职责分离**: App负责记录，脚本负责验证
3. **私有存储访问**: 通过ADB run-as命令安全访问App私有数据
4. **实时验证**: 支持立即验证用户刚执行的操作

## 故障排除

### 常见错误及解决方案

1. **ADB命令失败**
   - 检查设备是否正确连接
   - 确认USB调试模式已开启
   - 验证设备授权状态

2. **JSON文件不存在**
   - 确保App已启动过至少一次
   - 检查App是否正确初始化文件

3. **权限错误**
   - 确认使用的是正确的包名 `com.example.GaoDe`
   - 检查App是否为debug版本（run-as命令要求）

## 扩展说明

此测试框架可以轻松扩展以验证其他用户操作：
- 修改Python脚本中的`expected_action`参数
- 在App中添加更多操作记录点
- 支持验证操作序列而非单一操作